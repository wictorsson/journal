import "./EntryList.css";
export default function EntryList() {
  return (
    <div className="card">
      <h2>Mars 16, 2028</h2>
      <h3 className="card-title">"That's life in the city"</h3>
      <p>
        Build the UI of the Journal App using the components you have found.
        Remember to compose smaller components together to create bigger ones.
        Each component should be in its own file.
      </p>
    </div>
  );
}
